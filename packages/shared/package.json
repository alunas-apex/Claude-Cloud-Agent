{
  "name": "@claude-agent/shared",
  "version": "2.0.0",
  "private": true,
  "description": "Shared types, constants, and utilities for Claude Cloud Agent",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
